# Project identity

Sqush is the current project name.

## Current identity

- App name: Sqush
- Package name: `sqush`
- GitHub repo: `tavlean/sqush`
- Default branch: `main`
- Homepage metadata: `https://sqush.app`

## Historical identity

Sqush is derived from GoogleChromeLabs' Squoosh project. Keep that history clear in docs, credits, license notes, and architecture explanations. Do not rename every historical `Squoosh` reference to `Sqush`; many of those references explain upstream behavior or provenance.

The old `tavlean/SquooshPlus` fork is historical only. It is not the current project.

## Local folder note

This Codex workspace is currently mounted at `/Users/tav/Development/Tavlean/SquooshPlus`. That folder name is local machine state, not project branding. Rename it to `Sqush` only after closing this active workspace/session and reopening Codex from the new path, otherwise running tools may keep pointing at the old mounted path.
